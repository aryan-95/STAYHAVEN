# ─── API base URL ─────────────────────────────────────────────────────────────
# Leave empty to use Vite's proxy (recommended for local dev):
VITE_API_URL=

# Or point directly at the backend (needed when NOT using the proxy):
# VITE_API_URL=http://localhost:5000/api
